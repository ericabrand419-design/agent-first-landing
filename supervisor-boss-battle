<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Agent First: Supervisor Boss Battle</title>
<meta name="description" content="Ten calls, one shift. Handle it yourself or hit ASK SUPERVISOR. Every ask grows the boss meter. Can you finish the shift without summoning a monster you didn't need?">
<meta property="og:type" content="website">
<meta property="og:url" content="https://myagentfirst.com/supervisor-boss-battle.html">
<meta property="og:title" content="Agent First: Supervisor Boss Battle">
<meta property="og:description" content="Handle it yourself or hit ASK SUPERVISOR. Every ask grows the boss meter. Can you finish the shift without creating a monster?">
<meta property="og:image" content="https://myagentfirst.com/og-image.png">
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="Agent First: Supervisor Boss Battle">
<meta name="twitter:description" content="Handle it yourself or hit ASK SUPERVISOR. Every ask grows the boss meter.">
<meta name="twitter:image" content="https://myagentfirst.com/og-image.png">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;650;700;750;900&display=swap" rel="stylesheet">
<script async src="https://www.googletagmanager.com/gtag/js?id=G-TGQ8RPR5B1"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-TGQ8RPR5B1');
</script>
<style>
  :root{
    --ink:#171a2b;--muted:#6d728b;--line:#e5e4f1;--bg:#f7f6fc;--panel:rgba(255,255,255,.94);--soft:#faf9ff;
    --navy:#0b1025;--violet:#7c3aed;--purplebg:#f4efff;--coral:#ff6b54;--pink:#e447d5;
    --green:#177a55;--greenbg:#ecfbf4;--amber:#a15c00;--amberbg:#fff6e5;--danger:#c5364a;--dangerbg:#fdeaee;
    --radius-sm:9px;--radius-md:12px;--radius-lg:18px;--radius-full:999px;
    --space-3:12px;--space-4:16px;--space-5:24px;
    --shadow-panel:0 12px 34px rgba(64,47,112,.07),0 2px 6px rgba(20,22,45,.035);
    --font-sans:'Inter',ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;
    --grad-primary:linear-gradient(102deg,var(--coral) 0%,var(--pink) 47%,var(--violet) 100%);
    --grad-navy:linear-gradient(105deg,#090e21 0%,#0e1430 60%,#14103a 100%);
  }
  *{box-sizing:border-box}
  body{margin:0;font-family:var(--font-sans);color:var(--ink);background:var(--bg);-webkit-font-smoothing:antialiased}
  h1,h2{margin:0}
  a{text-decoration:none}
  p{margin:0}
  button{font-family:inherit}
  input{font-family:inherit}

  .ds-label{font-size:11px;line-height:1.4;font-weight:900;letter-spacing:.08em;text-transform:uppercase}
  .ds-body-sm{font-size:14px;line-height:1.55;font-weight:400}
  .ds-body{font-size:16px;line-height:1.55;font-weight:400}
  .ds-heading{font-size:18px;line-height:1.4;font-weight:900}
  .ds-score{font-size:28px;line-height:1;font-weight:900;letter-spacing:-.03em}
  .ds-hero{font-size:30px;line-height:1.1;font-weight:900;letter-spacing:-.02em}
  .ds-display{font-size:44px;line-height:1.08;font-weight:900;letter-spacing:-.015em}

  .wrap{width:100%;max-width:1100px;margin:0 auto}
  .section{width:100%;padding:88px 48px;display:flex;justify-content:center}
  .eyebrow-v{color:var(--violet)}
  .eyebrow-c{color:var(--coral)}
  .grid{display:grid;gap:var(--space-4)}

  .card{background:#fff;border:1px solid var(--line);border-radius:var(--radius-lg);padding:24px;box-shadow:var(--shadow-panel)}
  .card-on-dark{background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.16);border-radius:var(--radius-lg);padding:24px}

  .btn{display:inline-flex;align-items:center;gap:8px;padding:13px 26px;border-radius:var(--radius-md);font-weight:750;font-size:15px;cursor:pointer;border:0;transition:transform .14s ease,filter .14s ease}
  .btn:hover{transform:translateY(-1px);filter:brightness(1.03)}
  .btn-primary{background:var(--grad-primary);color:#fff;box-shadow:0 8px 20px rgba(176,57,217,.22)}
  .btn-ghost-dark{background:rgba(255,255,255,.06);color:#fff;border:1.5px solid rgba(255,255,255,.4)}
  .btn-ghost-dark:hover{border-color:#fff}
  .btn-ghost-light{background:var(--soft);color:var(--ink);border:1px solid var(--line)}

  .logo-mark{display:block;flex:0 0 auto}
  .brand-row{display:flex;align-items:center;gap:12px}
  .brand-word{font-size:11px;font-weight:900;letter-spacing:.2em}

  .field-input{width:100%;padding:12px 16px;border-radius:var(--radius-md);border:1.5px solid var(--line);background:#fff;color:var(--ink);font-family:var(--font-sans);font-size:15px;transition:border-color .14s ease}
  .field-input:focus{outline:none;border-color:var(--violet)}

  .pill{display:inline-flex;align-items:center;padding:4px 10px;border-radius:var(--radius-full);font-size:11px;font-weight:750;text-transform:uppercase;letter-spacing:.04em;flex:0 0 auto;}
  .pill-violet{background:var(--purplebg);color:var(--violet)}
  .pill-amber{background:var(--amberbg);color:var(--amber)}
  .pill-green{background:var(--greenbg);color:var(--green)}
  .pill-danger{background:var(--dangerbg);color:var(--danger)}

  .nsg-progress-track{width:100%;height:8px;border-radius:var(--radius-full);background:var(--purplebg);overflow:hidden;}
  .sbb-progress-fill{height:100%;border-radius:var(--radius-full);background:var(--grad-primary);width:0%;transition:width .35s ease;}
  .sbb-progress-label{font-size:12px;color:var(--muted);margin-bottom:10px;}

  .sbb-client-badge{display:inline-flex;align-items:center;padding:5px 12px;border-radius:var(--radius-full);background:var(--navy);color:#fff;font-size:12px;font-weight:750;}

  /* boss meter */
  .sbb-boss-wrap{display:flex;flex-direction:column;align-items:center;gap:10px;padding:20px;border-radius:var(--radius-lg);background:var(--navy);}
  .sbb-boss-stage{width:100%;height:130px;display:flex;align-items:center;justify-content:center;}
  .sbb-boss-blob{transition:transform .35s cubic-bezier(.34,1.4,.64,1),fill .35s ease;transform:scale(.55);}
  .sbb-boss-tier-0 .sbb-boss-blob{transform:scale(.5);}
  .sbb-boss-tier-1 .sbb-boss-blob{transform:scale(.68);}
  .sbb-boss-tier-2 .sbb-boss-blob{transform:scale(.85);}
  .sbb-boss-tier-3 .sbb-boss-blob{transform:scale(1.02);}
  .sbb-boss-tier-4 .sbb-boss-blob{transform:scale(1.2);}
  .sbb-boss-tier-5 .sbb-boss-blob{transform:scale(1.4);}
  @keyframes sbb-shake{0%,100%{transform:translateX(0)}20%{transform:translateX(-4px)}40%{transform:translateX(4px)}60%{transform:translateX(-3px)}80%{transform:translateX(3px)}}
  .sbb-shake{animation:sbb-shake .4s ease;}
  .sbb-boss-label{font-size:13px;font-weight:900;letter-spacing:.08em;text-transform:uppercase;color:#fff;}
  .sbb-boss-sub{font-size:12px;color:#8890ab;}

  .sbb-choice-row{display:flex;gap:12px;flex-wrap:wrap;margin-top:18px;}
  .sbb-choice-btn{flex:1;min-width:200px;padding:18px;border-radius:var(--radius-md);border:1.5px solid var(--line);background:#fff;font-family:var(--font-sans);font-weight:750;font-size:15px;cursor:pointer;text-align:center;transition:border-color .12s ease,background .12s ease,transform .1s ease;}
  .sbb-choice-btn:hover{transform:translateY(-1px);}
  .sbb-choice-self:hover{border-color:var(--violet);background:var(--purplebg);}
  .sbb-choice-ask{border-style:dashed;border-color:var(--amber);color:var(--amber);}
  .sbb-choice-ask:hover{background:var(--amberbg);}
  .sbb-choice-btn[disabled]{cursor:default;}
  .sbb-choice-btn.is-correct{border-color:var(--green);background:var(--greenbg);color:var(--ink);}
  .sbb-choice-btn.is-wrong{border-color:var(--danger);background:var(--dangerbg);color:var(--ink);}

  @keyframes sbb-fade-in{from{opacity:0;transform:translateY(6px);}to{opacity:1;transform:translateY(0);}}
  .sbb-fade{animation:sbb-fade-in .3s ease;}

  @media (max-width:900px){
    .section{padding:56px 20px}
    .ds-display{font-size:32px}
    .ds-hero{font-size:24px}
  }
</style>
</head>
<body>

<div style="width:100%;min-height:100vh;display:flex;flex-direction:column;">

  <!-- NAV -->
  <div style="width:100%;padding:18px 48px;display:flex;align-items:center;justify-content:space-between;background:#fff;border-bottom:1px solid var(--line);position:sticky;top:0;z-index:20;">
    <a href="index.html" class="brand-row">
      <svg class="logo-mark" viewBox="0 0 40 48" width="24" height="29" xmlns="http://www.w3.org/2000/svg"><defs><linearGradient id="afTopNav" x1="3" y1="18" x2="36" y2="3" gradientUnits="userSpaceOnUse"><stop stop-color="#FF8A3D"/><stop offset=".50" stop-color="#FF5B67"/><stop offset="1" stop-color="#F43BA5"/></linearGradient><linearGradient id="afBottomNav" x1="3" y1="45" x2="36" y2="27" gradientUnits="userSpaceOnUse"><stop stop-color="#6731F2"/><stop offset=".60" stop-color="#7C4DFF"/><stop offset="1" stop-color="#A345F0"/></linearGradient></defs><path d="M5 18.5 20 4.5 35 18.5" fill="none" stroke="url(#afTopNav)" stroke-width="7.5" stroke-linecap="round" stroke-linejoin="round"/><path d="M5 43 20 29 35 43" fill="none" stroke="url(#afBottomNav)" stroke-width="7.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
      <div class="brand-word" style="color:var(--ink)">AGENT FIRST</div>
    </a>
    <div style="display:flex;align-items:center;gap:20px;">
      <a href="labs.html" class="ds-body-sm nav-link" style="color:var(--ink);font-weight:650;">Free Tools</a>
      <a href="agent-challenge.html" class="ds-body-sm nav-link" style="color:var(--ink);font-weight:650;">Agent Challenge</a>
      <a href="pricing.html" class="ds-body-sm nav-link" style="color:var(--ink);font-weight:650;">Pricing</a>
      <a href="index.html#contact" class="btn btn-primary" style="padding:9px 20px;font-size:14px;">Become a pilot partner</a>
    </div>
  </div>

  <!-- INTRO -->
  <div class="section" id="introStage" style="background:var(--grad-navy);">
    <div class="wrap" style="display:flex;flex-direction:column;gap:20px;max-width:720px;align-items:center;text-align:center;">
      <div class="ds-label eyebrow-c">Agent First Labs &middot; 10 calls</div>
      <h1 class="ds-display" style="color:#fff;">Supervisor Boss Battle</h1>
      <p class="ds-body" style="color:#c7cce0;">Ten calls, one shift. For each one: handle it yourself, or hit ASK SUPERVISOR. Every ask grows the boss meter, whether you actually needed it or not. Some calls genuinely do need a supervisor. Most don't. Can you tell which is which?</p>
      <button class="btn btn-primary" id="startBtn" style="padding:15px 34px;font-size:16px;">Start the shift</button>
    </div>
  </div>

  <!-- GAME -->
  <div class="section" id="gameStage" style="background:var(--soft);display:none;">
    <div class="wrap" style="display:flex;flex-direction:column;gap:20px;max-width:640px;">

      <div class="sbb-progress-label" id="progressLabel">Call 1 of 10</div>
      <div class="nsg-progress-track" style="margin-top:-12px;"><div class="sbb-progress-fill" id="progressFill"></div></div>

      <div class="sbb-boss-wrap" id="bossWrap">
        <div class="sbb-boss-stage">
          <svg class="sbb-boss-blob" id="bossBlob" width="90" height="90" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
            <circle cx="50" cy="52" r="40" fill="#7c3aed"/>
            <circle cx="36" cy="46" r="7" fill="#fff"/>
            <circle cx="64" cy="46" r="7" fill="#fff"/>
            <circle cx="36" cy="46" r="3.2" fill="#0b1025"/>
            <circle cx="64" cy="46" r="3.2" fill="#0b1025"/>
            <path d="M34 66 Q50 78 66 66" stroke="#0b1025" stroke-width="4" fill="none" stroke-linecap="round"/>
          </svg>
        </div>
        <div class="sbb-boss-label" id="bossLabel">Dormant</div>
        <div class="sbb-boss-sub" id="bossSub">No interruptions yet</div>
      </div>

      <div class="card sbb-fade" id="questionCard">
        <div style="margin-bottom:12px;"><span class="sbb-client-badge" id="clientBadge">--</span></div>
        <div class="ds-heading" id="situationText" style="margin-bottom:8px;">&nbsp;</div>

        <div class="sbb-choice-row" id="choiceRow">
          <button class="sbb-choice-btn sbb-choice-self" id="selfBtn">Handle it myself</button>
          <button class="sbb-choice-btn sbb-choice-ask" id="askBtn">ASK SUPERVISOR</button>
        </div>

        <div id="feedback" class="ds-body-sm" style="display:none;margin-top:16px;padding-top:16px;border-top:1px solid var(--line);"></div>
        <button class="btn btn-primary" id="nextBtn" style="display:none;margin-top:16px;">Next call</button>
      </div>

    </div>
  </div>

  <!-- RESULTS -->
  <div class="section" id="resultsStage" style="background:#fff;display:none;">
    <div class="wrap" style="display:flex;flex-direction:column;gap:24px;max-width:640px;">

      <div class="card">
        <div class="ds-label eyebrow-c" style="margin-bottom:10px;">Shift complete</div>
        <div class="ds-hero" id="resultsHeadline" style="margin-bottom:16px;">--</div>
        <div class="grid" style="grid-template-columns:repeat(auto-fit,minmax(150px,1fr));">
          <div><div class="ds-score" id="rCorrect">0/10</div><div class="ds-body-sm" style="color:var(--muted);">Right call</div></div>
          <div><div class="ds-score" style="color:var(--amber);" id="rUnnecessary">0</div><div class="ds-body-sm" style="color:var(--muted);">Unnecessary escalations</div></div>
          <div><div class="ds-score" style="color:var(--danger);" id="rMissed">0</div><div class="ds-body-sm" style="color:var(--muted);">Times you should have asked but didn't</div></div>
        </div>
      </div>

      <div class="card">
        <div class="ds-body" style="font-weight:750;margin-bottom:10px;">Share your result</div>
        <div style="display:flex;gap:10px;flex-wrap:wrap;">
          <input class="field-input" id="shareText" readonly style="flex:1;min-width:220px;">
          <button class="btn btn-ghost-light" id="copyBtn">Copy</button>
        </div>
        <div class="ds-body-sm" id="copyStatus" style="color:var(--green);display:none;margin-top:8px;">Copied.</div>
      </div>

      <div style="display:flex;gap:12px;flex-wrap:wrap;">
        <button class="btn btn-primary" id="playAgainBtn">Play again</button>
        <a href="supervisor-tax-calculator.html" class="btn btn-ghost-light">See what this costs for real: Supervisor Tax Calculator</a>
      </div>

      <div class="card-on-dark" style="flex-direction:column;gap:14px;background:var(--navy);border:none;display:flex;">
        <div class="ds-body" style="font-weight:900;color:#fff;">This is why escalation is part of the decision, not a separate hunt for a person.</div>
        <p class="ds-body-sm" style="color:#c7cce0;">When a request needs a supervisor, Agent First surfaces that as part of the decision itself, ESCALATE, instead of leaving an agent to guess whether this is one of those situations.</p>
        <div style="display:flex;gap:12px;flex-wrap:wrap;">
          <a href="pilot-program.html" class="btn btn-primary">See what a pilot includes</a>
          <a href="sample-client-profile.html" class="btn btn-ghost-dark">Build my sample Client Profile</a>
        </div>
      </div>

      <div class="card" id="emailCard">
        <div class="ds-body" style="font-weight:750;margin-bottom:10px;">Want this result emailed to you?</div>
        <form id="emailForm" style="display:flex;gap:10px;flex-wrap:wrap;">
          <input type="hidden" name="access_key" value="7207f464-b77a-4a0c-b6b7-3a747ab11496">
          <input type="hidden" name="subject" value="Supervisor Boss Battle result, Agent First">
          <input type="hidden" name="from_name" value="Agent First website">
          <input type="hidden" name="result_summary" id="resultSummaryField" value="">
          <input type="checkbox" name="botcheck" style="display:none" tabindex="-1" autocomplete="off">
          <input class="field-input" style="flex:1;min-width:220px;" type="email" name="email" placeholder="Your work email" required>
          <button type="submit" class="btn btn-primary">Email it to me</button>
        </form>
        <div id="emailStatus" class="ds-body-sm" style="display:none;margin-top:10px;"></div>
      </div>

    </div>
  </div>

  <!-- FOOTER -->
  <div style="width:100%;padding:28px 48px;display:flex;align-items:center;justify-content:space-between;background:#fff;border-top:1px solid var(--line);flex-wrap:wrap;gap:12px;">
    <a href="index.html" class="brand-row">
      <svg class="logo-mark" viewBox="0 0 40 48" width="18" height="22" xmlns="http://www.w3.org/2000/svg"><defs><linearGradient id="afTopFoot" x1="3" y1="18" x2="36" y2="3" gradientUnits="userSpaceOnUse"><stop stop-color="#FF8A3D"/><stop offset=".50" stop-color="#FF5B67"/><stop offset="1" stop-color="#F43BA5"/></linearGradient><linearGradient id="afBottomFoot" x1="3" y1="45" x2="36" y2="27" gradientUnits="userSpaceOnUse"><stop stop-color="#6731F2"/><stop offset=".60" stop-color="#7C4DFF"/><stop offset="1" stop-color="#A345F0"/></linearGradient></defs><path d="M5 18.5 20 4.5 35 18.5" fill="none" stroke="url(#afTopFoot)" stroke-width="7.5" stroke-linecap="round" stroke-linejoin="round"/><path d="M5 43 20 29 35 43" fill="none" stroke="url(#afBottomFoot)" stroke-width="7.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
      <div class="brand-word" style="color:var(--ink);font-size:12px;">AGENT FIRST</div>
    </a>
    <div class="ds-body-sm" style="color:var(--muted);">Keep the human. Upgrade the agent.</div>
  </div>

</div>

<script>
(function () {
  'use strict';
  var $ = function (id) { return document.getElementById(id); };

  var CLIENT_NAMES = { A: 'Harborview', B: 'Meridian', C: 'Northgate' };

  var TIER_NAMES = ['Dormant', 'Slack message', 'Hold time', 'Callback', 'Escalation', 'QA review'];
  var TIER_COLORS = ['#7c3aed', '#7c3aed', '#e447d5', '#ff6b54', '#c5364a', '#8a0f24'];

  var SCENARIOS = [
    { client: 'A', situation: 'A customer wants a $180 refund processed.', requiresSupervisor: false, why: 'Harborview only requires sign-off over $200. This one is under the line, an agent can process it directly.' },
    { client: 'B', situation: 'A customer wants a $410 refund processed.', requiresSupervisor: false, why: 'Meridian never requires sign-off on refunds, any amount, any agent.' },
    { client: 'C', situation: 'A customer wants a $275 refund processed.', requiresSupervisor: true, why: 'Northgate requires sign-off over $200. This one needs it.' },
    { client: 'B', situation: 'A customer wants to cancel an appointment scheduled for tomorrow, and asks about a fee.', requiresSupervisor: false, why: 'This is a standard, published rule (a $25 fee applies). No approval needed to quote it.' },
    { client: 'A', situation: 'A customer\'s account shows a service credit that is not explained anywhere in their file.', requiresSupervisor: true, why: 'An unexplained credit with no documentation behind it needs a second set of eyes before anyone touches it.' },
    { client: 'C', situation: 'A customer asks to combine two separate promotional discounts on one order.', requiresSupervisor: false, why: 'The rule against stacking discounts is already written down. No approval needed, just explain it.' },
    { client: 'A', situation: 'A customer is threatening to cancel their whole account unless they get a discount that does not exist in the system.', requiresSupervisor: true, why: 'A customer asking for something outside any written policy, with account risk attached, is exactly what escalation exists for.' },
    { client: 'B', situation: 'A customer asks whether ID verification is required just to check their account balance.', requiresSupervisor: false, why: 'Meridian\'s rule is published and simple: not required for general questions. No need to check with anyone.' },
    { client: 'C', situation: 'A customer wants same-day service outside normal service hours, something with genuinely no written rule either way.', requiresSupervisor: true, why: 'When nothing is written down and the customer is pushing for an exception, that is a decision above an agent\'s own authority.' },
    { client: 'B', situation: 'A customer wants to reschedule for the third time this month.', requiresSupervisor: true, why: 'Meridian requires a supervisor note after the second reschedule in a month. This is the third.' }
  ];

  var introStage = document.getElementById('introStage'), gameStage = document.getElementById('gameStage'), resultsStage = document.getElementById('resultsStage');
  var state = null;

  function startGame() {
    state = { index: 0, correct: 0, unnecessary: 0, missed: 0, bossTier: 0, locked: false };
    introStage.style.display = 'none';
    resultsStage.style.display = 'none';
    gameStage.style.display = 'flex';
    updateBoss();
    renderQuestion();
    window.scrollTo({ top: 0, behavior: 'smooth' });
  }

  function updateBoss() {
    var wrap = $('bossWrap');
    wrap.className = 'sbb-boss-wrap sbb-boss-tier-' + state.bossTier;
    $('bossLabel').textContent = TIER_NAMES[state.bossTier];
    $('bossSub').textContent = state.bossTier === 0 ? 'No interruptions yet' : (state.bossTier + ' interruption' + (state.bossTier === 1 ? '' : 's') + ' so far');
    $('bossBlob').querySelector('circle').setAttribute('fill', TIER_COLORS[state.bossTier]);
  }

  function growBoss() {
    state.bossTier = Math.min(5, state.bossTier + 1);
    updateBoss();
    var wrap = $('bossWrap');
    wrap.classList.add('sbb-shake');
    setTimeout(function () { wrap.classList.remove('sbb-shake'); }, 420);
  }

  function renderQuestion() {
    var s = SCENARIOS[state.index];
    state.locked = false;
    $('progressLabel').textContent = 'Call ' + (state.index + 1) + ' of ' + SCENARIOS.length;
    $('progressFill').style.width = ((state.index / SCENARIOS.length) * 100) + '%';
    $('clientBadge').textContent = CLIENT_NAMES[s.client] + ' account';
    $('situationText').textContent = s.situation;
    $('feedback').style.display = 'none';
    $('nextBtn').style.display = 'none';
    $('selfBtn').disabled = false;
    $('askBtn').disabled = false;
    $('selfBtn').classList.remove('is-correct', 'is-wrong');
    $('askBtn').classList.remove('is-correct', 'is-wrong');

    var card = $('questionCard');
    card.classList.remove('sbb-fade');
    void card.offsetWidth;
    card.classList.add('sbb-fade');
  }

  function choose(choseAsk) {
    if (state.locked) return;
    state.locked = true;
    var s = SCENARIOS[state.index];
    var wasCorrect = choseAsk === s.requiresSupervisor;

    $('selfBtn').disabled = true;
    $('askBtn').disabled = true;

    var btn = choseAsk ? $('askBtn') : $('selfBtn');
    btn.classList.add(wasCorrect ? 'is-correct' : 'is-wrong');

    if (wasCorrect) {
      state.correct += 1;
    } else if (choseAsk && !s.requiresSupervisor) {
      state.unnecessary += 1;
    } else if (!choseAsk && s.requiresSupervisor) {
      state.missed += 1;
    }

    if (choseAsk) growBoss();

    var feedback = $('feedback');
    feedback.style.display = 'block';
    var badge = wasCorrect ? '<span class="pill pill-green" style="margin-bottom:8px;">Right call</span><br>' : '<span class="pill pill-danger" style="margin-bottom:8px;">Wrong call</span><br>';
    feedback.innerHTML = badge + s.why;

    $('nextBtn').style.display = 'inline-flex';
    $('nextBtn').textContent = state.index < SCENARIOS.length - 1 ? 'Next call' : 'See my results';
  }

  $('selfBtn').addEventListener('click', function () { choose(false); });
  $('askBtn').addEventListener('click', function () { choose(true); });

  $('nextBtn').addEventListener('click', function () {
    state.index += 1;
    if (state.index >= SCENARIOS.length) {
      endGame();
    } else {
      renderQuestion();
    }
  });

  function endGame() {
    gameStage.style.display = 'none';
    resultsStage.style.display = 'flex';

    var headline;
    if (state.unnecessary === 0 && state.missed === 0) {
      headline = 'Perfect shift. ' + state.correct + ' of ' + SCENARIOS.length + ' right, and the boss meter only grew when it genuinely had to.';
    } else if (state.unnecessary > 0) {
      headline = 'You finished the shift, but summoned the supervisor unnecessarily ' + state.unnecessary + ' time' + (state.unnecessary === 1 ? '' : 's') + '.';
    } else {
      headline = state.correct + ' of ' + SCENARIOS.length + ' right calls this shift.';
    }
    $('resultsHeadline').textContent = headline;
    $('rCorrect').textContent = state.correct + '/' + SCENARIOS.length;
    $('rUnnecessary').textContent = state.unnecessary;
    $('rMissed').textContent = state.missed;

    var shareLine = 'I made it through Supervisor Boss Battle with ' + state.correct + '/' + SCENARIOS.length + ' right calls and ' + state.unnecessary + ' unnecessary escalation' + (state.unnecessary === 1 ? '' : 's') + '. Try it: myagentfirst.com/supervisor-boss-battle.html';
    $('shareText').value = shareLine;
    $('resultSummaryField').value = shareLine;

    window.scrollTo({ top: 0, behavior: 'smooth' });
  }

  $('startBtn').addEventListener('click', startGame);
  $('playAgainBtn').addEventListener('click', function () {
    resultsStage.style.display = 'none';
    startGame();
  });

  $('copyBtn').addEventListener('click', function () {
    var field = $('shareText');
    field.select();
    var done = function () {
      $('copyStatus').style.display = 'block';
      setTimeout(function () { $('copyStatus').style.display = 'none'; }, 1800);
    };
    if (navigator.clipboard && navigator.clipboard.writeText) {
      navigator.clipboard.writeText(field.value).then(done).catch(function () {
        try { document.execCommand('copy'); done(); } catch (e) { /* clipboard unavailable */ }
      });
    } else {
      try { document.execCommand('copy'); done(); } catch (e) { /* clipboard unavailable */ }
    }
  });

  var emailForm = $('emailForm');
  emailForm.addEventListener('submit', function (e) {
    e.preventDefault();
    var btn = emailForm.querySelector('button[type="submit"]');
    var status = $('emailStatus');
    var original = btn.textContent;
    btn.disabled = true;
    btn.textContent = 'Sending...';
    fetch('https://api.web3forms.com/submit', {
      method: 'POST',
      headers: { 'Accept': 'application/json' },
      body: new FormData(emailForm)
    }).then(function (r) { return r.json(); }).then(function (data) {
      if (data && data.success) {
        emailForm.style.display = 'none';
        status.style.color = '#1f5c42';
        status.textContent = "Sent. I'll follow up directly too.";
        status.style.display = 'block';
      } else {
        btn.disabled = false;
        btn.textContent = original;
        status.style.color = '#c53636';
        status.textContent = 'Something went wrong. Please try again, or email info@myagentfirst.com directly.';
        status.style.display = 'block';
      }
    }).catch(function () {
      btn.disabled = false;
      btn.textContent = original;
      status.style.color = '#c53636';
      status.textContent = 'Something went wrong. Please try again, or email info@myagentfirst.com directly.';
      status.style.display = 'block';
    });
  });
})();
</script>

</body>
</html>
